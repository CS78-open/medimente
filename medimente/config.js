// config.js

// La chiave API di Google Gemini è ottenuta dalla variabile d'ambiente process.env.API_KEY.
// Non è necessario modificare questo file per configurare la chiave API.
// Assicurati che la variabile d'ambiente API_KEY sia impostata nel tuo ambiente di esecuzione
// prima di avviare l'applicazione.

export const API_KEY = process.env.API_KEY;