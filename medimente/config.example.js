// config.example.js

// ISTRUZIONI IMPORTANTI PER LA CONFIGURAZIONE:
// 1. DUPLICA questo file.
// 2. Rinomina la copia in `config.js`.
// 3. Inserisci la tua chiave API di Google Gemini nel file `config.js`.

// ATTENZIONE: Il file `config.js` è ignorato da Git (vedi .gitignore)
// per proteggere la tua chiave. NON RIMUOVERE `config.js` da .gitignore.

export const API_KEY = 'INCOLLA_LA_TUA_CHIAVE_API_QUI';
